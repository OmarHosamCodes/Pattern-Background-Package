name: pattern_background
description: Create Pattern Backgrounds
version: 1.1.2
homepage: https://github.com/OmarHosamCodes/pattern-background-package

environment:
  sdk: ">=3.0.5 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
